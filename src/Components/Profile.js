import React from 'react';

const Profile = (props) => {
    return (
        <div>
            <h1>My profile</h1>
            <a href="/settings">Settings</a>
        </div>
    );
}

export default Profile;